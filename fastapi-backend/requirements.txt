fastapi==0.136.0
uvicorn==0.44.0
python-multipart==0.0.26

pydantic==2.10.4
pydantic-settings==2.7.0

sqlalchemy==2.0.36
pgvector==0.3.6
psycopg2-binary==2.9.10

sentence-transformers==5.4.1
langchain-text-splitters==0.3.2
torch==2.2.2
transformers==4.57.1

python-dotenv==1.2.2
groq==1.1.2

pdfplumber==0.11.9
python-docx==1.2.0
PyMuPDF==1.27.2

assemblyai>=0.62.0        # STT — upgraded: 0.37 used deprecated speech_model, 0.62+ requires speech_models (plural)
faster-whisper==1.1.1    # fallback STT — runs locally, no API key needed

cartesia==3.0.2          # primary TTS (free trial at cartesia.ai)
edge-tts==6.1.12         # fallback TTS — Microsoft neural voices, free, no API key
                         # pinned to 6.x: v7+ uses a DRM token (Sec-MS-GEC) that
                         # Microsoft's servers currently reject with 403

pydub==0.25.1            # audio format conversion
soundfile==0.13.1        # read/write audio files

crewai==0.70.0
crewai-tools>=1.0.0

